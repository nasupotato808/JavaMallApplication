spring:
    application:
      name: mall

datasource:
    url: jdbc:postgresql://localhost:5432/mall
    username: mall
    password: mall

jpa:
    hibernate:
        ddl-auto: update
    show-sql: true
    properties:
        hibernate:
            format_sql: true

server:
    port: 8080

